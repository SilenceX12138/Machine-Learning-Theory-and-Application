data_path = './data/'
model_path = './checkpoints/model.pth'  # the path where checkpoint saved

VAL_RATIO = 0.2
BATCH_SIZE = 64

num_epoch = 20  # number of training epoch
learning_rate = 0.0001  # learning rate

SEED = 0
